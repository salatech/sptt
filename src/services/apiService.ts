export const startWebSocketStream = () => {
	// WebSocket connection and streaming logic here
};

export const stopWebSocketStream = () => {
	// Close WebSocket connection here
};

export const startGrpcStream = () => {
	// gRPC connection and streaming logic here
};

export const stopGrpcStream = () => {
	// Close gRPC connection here
};
